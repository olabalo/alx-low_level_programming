#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _puts - Prints a string to stdout
 * @s: Pointer to string to print
 */
void _puts(char *s)
{
puts(s);
}
/**
 * main - Entry point
 * Return: Always 0 (success)
 */
int main(void)
{
/* Main code goes here */
return (0);
}
